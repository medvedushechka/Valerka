# 🚀 Инструкция по деплою на Timeweb

## Шаг 1: Регистрация и заказ VPS

1. Перейдите на [timeweb.com](https://timeweb.com)
2. Зарегистрируйтесь или войдите в аккаунт
3. Выберите **VPS/VDS** → **Заказать сервер**
4. Выберите конфигурацию:
   - **ОС:** Ubuntu 22.04 LTS (рекомендуется)
   - **RAM:** 2 ГБ
   - **CPU:** 1 vCPU
   - **Диск:** 20-25 ГБ SSD
   - **Панель управления:** ISPmanager (упрощает настройку)
5. Оплатите и дождитесь активации (обычно 5-15 минут)

## Шаг 2: Получение данных доступа

После активации вы получите:
- IP-адрес сервера
- Логин: `root`
- Пароль для SSH
- Ссылку на панель ISPmanager (если выбрали)

## Шаг 3: Подключение к серверу

### Вариант A: Через ISPmanager (проще)

1. Откройте панель ISPmanager по ссылке из письма
2. Войдите с данными из письма
3. Перейдите в **WWW** → **WWW-домены**
4. Создайте новый домен или используйте IP-адрес

### Вариант B: Через SSH (для опытных)

**Windows (PowerShell):**
```powershell
ssh root@ВАШ_IP_АДРЕС
```

**Установка веб-сервера Nginx:**
```bash
apt update
apt install nginx -y
systemctl start nginx
systemctl enable nginx
```

## Шаг 4: Загрузка файлов проекта

### Способ 1: Через FileZilla (рекомендуется для новичков)

1. Скачайте [FileZilla Client](https://filezilla-project.org/)
2. Подключитесь:
   - **Хост:** ВАШ_IP_АДРЕС
   - **Имя пользователя:** root
   - **Пароль:** ваш пароль
   - **Порт:** 22
   - **Протокол:** SFTP
3. Перейдите в папку `/var/www/html/` (или путь из ISPmanager)
4. Загрузите все файлы проекта:
   - `index.html`
   - `style.css`
   - `script.js`
   - `birthday-photo.jpg`

### Способ 2: Через ISPmanager

1. В панели ISPmanager откройте **Файловый менеджер**
2. Перейдите в папку вашего домена
3. Загрузите все файлы через кнопку **Загрузить**

### Способ 3: Через Git (если используете)

```bash
cd /var/www/html/
git clone ВАШ_РЕПОЗИТОРИЙ .
```

## Шаг 5: Настройка прав доступа

Если используете SSH:
```bash
cd /var/www/html/
chmod 644 *.html *.css *.js *.jpg
chown -R www-data:www-data /var/www/html/
```

## Шаг 6: Проверка работы

1. Откройте браузер
2. Введите IP-адрес вашего сервера: `http://ВАШ_IP_АДРЕС`
3. Должна открыться страница с поздравлением

## Шаг 7: Подключение домена (опционально)

### Если у вас есть домен:

1. В панели управления доменом (например, reg.ru) добавьте A-запись:
   - **Тип:** A
   - **Имя:** @ (или www)
   - **Значение:** IP-адрес вашего VPS
   - **TTL:** 3600

2. В ISPmanager:
   - Перейдите в **WWW** → **WWW-домены**
   - Нажмите **Создать**
   - Введите ваш домен
   - Укажите путь к файлам проекта

3. Подождите 1-24 часа для распространения DNS

### Настройка SSL (HTTPS):

В ISPmanager:
1. Перейдите в **WWW** → **SSL-сертификаты**
2. Выберите **Let's Encrypt**
3. Укажите ваш домен
4. Нажмите **Получить сертификат**

## Структура файлов на сервере

```
/var/www/html/
├── index.html
├── style.css
├── script.js
└── birthday-photo.jpg
```

## Полезные команды (для SSH)

**Перезапуск Nginx:**
```bash
systemctl restart nginx
```

**Просмотр логов ошибок:**
```bash
tail -f /var/log/nginx/error.log
```

**Проверка статуса Nginx:**
```bash
systemctl status nginx
```

## Обновление проекта

Просто замените файлы через FileZilla или файловый менеджер ISPmanager.

## Стоимость

- **VPS 2 ГБ RAM:** ~200-300₽/мес
- **Домен .ru:** ~200-300₽/год (опционально)
- **SSL-сертификат:** бесплатно (Let's Encrypt)

## Поддержка

- Техподдержка Timeweb: 24/7 через тикеты в личном кабинете
- Телефон: 8 (800) 700-06-08
- База знаний: [timeweb.com/ru/help](https://timeweb.com/ru/help)

## Альтернативы для нескольких проектов

На одном VPS можно разместить несколько сайтов:
1. Создайте поддомены или используйте разные папки
2. В ISPmanager: **WWW** → **WWW-домены** → **Создать**
3. Укажите разные пути для каждого проекта

**Пример:**
- `project1.yourdomain.ru` → `/var/www/project1/`
- `project2.yourdomain.ru` → `/var/www/project2/`
- Или по IP: `http://IP/project1/`, `http://IP/project2/`

---

**Готово!** Ваш проект теперь доступен в интернете 🎉
